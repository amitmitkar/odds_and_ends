#include <stdio.h>

int *psum(struct abc *pabc, int a, void *b);
struct pqr  *spsum(struct abc *pabc, int a, void *b);
char  *cpsum(struct abc *pabc, int a, void *b);
struct jpr  jpsum(struct abc *pabc, int a, void *b);
int sum(struct abc *pabc, int a, void *b);

int main(int argc, char **argv)
{
	return 0;
}
